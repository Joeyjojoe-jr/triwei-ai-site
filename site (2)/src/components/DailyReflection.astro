---
import { getEntry } from "../lib/getDaily";
const entry = getEntry();
---
<div class="card">
  {entry ? (
    <article>
      <h3>{entry.date}</h3>
      <div set:html={entry.body} />
      <p><a href="/archive">See archive →</a></p>
    </article>
  ) : (
    <p>No entry yet. Add a file into <kbd>app/runs/</kbd> then rebuild the site.</p>
  )}
</div>
