---
const { id, title, lead = "", children } = Astro.props;
---
<section id={id} class="section">
  <h2>{title}</h2>
  {lead && <p class="lead">{lead}</p>}
  <div class="grid two">
    <div>
      <slot />
    </div>
    <div>
      <slot name="side" />
    </div>
  </div>
</section>
